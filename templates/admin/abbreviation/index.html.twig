{% extends 'admin/layout.html.twig' %}

{% block title 'Zkratky' %}

{% block content %}
    <div class="box">
        <div class="box-body">
            <table class="table table-striped table-hover js-data-table">
                <thead>
                <tr>
                    <th>Content</th>
                    <th>Title</th>
                    <th>Description</th>
                </tr>
                </thead>
                <tbody>
                {% for abbreviation in abbreviations %}
                    <tr>
                        <td>
                            <a href="{{ path('admin.abbreviation.edit', {id: abbreviation.id}) }}">
                                {{ abbreviation.content }}
                            </a>
                        </td>
                        <td>{{ abbreviation.title }}</td>
                        <td>{{ abbreviation.description }}</td>
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                <tr>
                    <th>Content</th>
                    <th>Title</th>
                    <th>Description</th>
                </tr>
                </tfoot>
            </table>
        </div>
        <div class="box-footer">
            <a href="{{ path('admin.abbreviation.add') }}" class="btn btn-primary">
                <i class="fa fa-plus"></i> Add abbreviation
            </a>
        </div>
    </div>
{% endblock %}
