{% extends 'admin/layout.html.twig' %}

{% block title 'Typy zdrojů' %}

{% block content %}
    <div class="box">
        <div class="box-body">
            <table class="table table-striped table-hover js-data-table">
                <thead>
                    <tr>
                        <th>{{ 'admin.title'|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for sourceType in sourceTypes %}
                        <tr>
                            <td>
                                <a href="{{ path('admin.source-type.edit', {id: sourceType.id}) }}">
                                    {{ sourceType.title }}
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
                <tfoot>
                    <tr>
                        <th>{{ 'admin.title'|trans }}</th>
                    </tr>
                </tfoot>
            </table>
        </div>
        <div class="box-footer">
            <a href="{{ path('admin.source-type.add') }}" class="btn btn-primary">
                <i class="fa fa-plus"></i> {{ 'admin.button.add_source_type'|trans }}
            </a>
        </div>
    </div>
{% endblock %}
