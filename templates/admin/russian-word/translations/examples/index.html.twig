{% extends 'admin/layout.html.twig' %}

{% block title 'Ruská slova' %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block content %}
    <div class="box">
        <div class="box-header with-border">
            <h3 class="box-title">Examples</h3>
        </div>
        <div class="box-body">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Russian word</th>
                        <th>Russian sentence</th>
                        <th>Czech sentence</th>
                        <th>Czech word</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    {% for example in translation.examples %}
                        <tr>
                            <td>
                                <a href="{{ path('admin.russian-word.edit', {id: example.translation.russianWord.id}) }}">
                                    {{ example.translation.russianWord.content }}
                                </a>
                            </td>
                            <td>
                                {{ example.russianWordSentence }}
                            </td>
                            <td>
                                {{ example.czechWordSentence }}
                            </td>
                            <td>
                                <a href="{{ path('admin.czech-word.edit', {id: example.translation.czechWord.id}) }}">
                                    {{ example.translation.czechWord.content }}
                                </a>
                            </td>
                            <td>
                                <a href="{{ path('admin.russian-word.translations.examples.edit', {
                                    id: word.id,
                                    translationId: translation.id,
                                    translationExampleId: example.id
                                }) }}" class="btn btn-primary btn-xs">
                                    Edit
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
                <tfoot>
                    <tr>
                        <th>Russian word</th>
                        <th>Russian sentence</th>
                        <th>Czech sentence</th>
                        <th>Czech word</th>
                        <th>Action</th>
                    </tr>
                </tfoot>
            </table>
        </div>
        <div class="box-footer">
            <a href="{{ path('admin.russian-word.translations', { id: word.id }) }}" class="btn btn-default">
                Back to translations
            </a>
        </div>
    </div>

    {{ include('admin/_fragments/box/translation_example_add.html.twig') }}
{% endblock %}
