{% extends 'app/layout.html.twig' %}

{% block heading 'Zkratky' %}

{% block content %}
    <table class="table table-striped table-hover table-bordered">
        <thead>
            <tr>
                <th>Zkratka</th>
                <th>Význam</th>
            </tr>
        </thead>
        <tbody>
            {% for abbreviation in abbreviations %}
                <tr>
                    <td><em>{{ abbreviation.content }}</em></td>
                    <td>{{ abbreviation.title }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
